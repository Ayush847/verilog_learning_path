module bcd_to_excess3 (
    input a, b, c, d,
    output w, x, y, z
);
    assign w = a | (b & c) | (b & d);
    assign x = b ^ c;
    assign y = (~b & c & d) | (b & ~c & d) | (b & c & ~d) | (~b & ~c & d);
    assign z = ~d;
endmodule
